<% unless (@item.video.nil? || @item.video.empty?) %>
  <div id="main-video" class="youtube" data-key="<%= youtube_key(@item.video) %>"></div>
<% else %>
  <div class="image main-image" data-image="<%= @item.image %>"><img></div>
<% end %>
<h2><%= @item.title %></h2>
