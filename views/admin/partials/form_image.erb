<label>
  <span><%= key %>: </span>
  <input name="image[<%= key %>]"
         type="text"
         onchange="onChangeText(this, '<%= key %>')"
         value="<%= item.image.send(key.to_sym) %>"
         />
</label>
