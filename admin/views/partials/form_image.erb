<label>
  <span><%= key %>: </span>
  <input name="entry[<%= key %>]"
         type="text"
         onchange="onChangeText(this, '<%= key %>')"
         value="<%= item.send(key.to_sym) %>"
         />
</label>
