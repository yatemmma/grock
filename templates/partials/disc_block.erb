<a class="block disc" href="<%= link("disc/#{disc.code}.html") %>">
  <div class="image" data-image="<%= disc.image %>">
    <img>
  </div>
  <div class="desc"><%= disc.name %></div>
  <div class="desc"><%= disc.band_name %></div>
  <div class="desc"><%= disc.date %></div>
</a>
