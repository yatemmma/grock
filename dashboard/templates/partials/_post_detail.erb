<div id="post">
  <% if post.has_media? %>
    <%= post.media %>
  <% end %>
  <h1 class="post-title center"><%= post['title'] %></h1>
  <div class="content center">
    <%= post.body(bands) %>
  </div>
</div>
