<% member = obj; level = 1 %>
<html>
<head>
  <%= head(member.name + " | G-ROCK", level) %>
</head>
<body>
  <%= header(1) %>
  <div class="contents-wrapper">

    <div class="main-image image" data-image="<%= member.images.nil? ? '' : member.images.first %>">
      <img>
    </div>
    <h2>
      <%= member.name %>
      <%= " (#{member.description})" unless member.description.nil? %>
    </h2>

    <div class="contents">
      <%= body(member.body, level) %>

      <%= sns("member/#{member.code}.html", member.name, level) %>

      <%= origin_block(member) unless member.country.nil?%>

      <% (member.links || []).map do |link| %>
        <%= link_block("Link", link) %>
      <% end %>
    </div>

    <div class="sub-contents">
      xxxx
    </div>
  </div>
  <%= footer(level) %>
</body>
</html>
